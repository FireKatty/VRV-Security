{
  "buildCommand": "cd rbac-ui && npm install && npm run build",
  "outputDirectory": "rbac-ui/build",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/backend/(.*)",
      "destination": "/api/server.js"
    }
  ]
}
